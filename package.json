{
  "name": "dheeraj-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Dheeraj Kumar \u2014 Web & Software Developer. Portfolio as a product.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "build:lab": "tsx scripts/build-lab-data.ts",
    "check:content": "tsx scripts/check-content.ts",
    "check:budget": "tsx scripts/check-budget.ts",
    "check:retrieval": "tsx scripts/check-retrieval.ts",
    "ask:gaps": "tsx scripts/ask-gaps.ts",
    "check:graph": "tsx scripts/check-graph.ts",
    "verify": "npm run typecheck && npm run check:content && npm run check:graph && npm run check:retrieval && npm run build",
    "test:e2e": "playwright test",
    "verify:full": "npm run typecheck && npm run check:content && npm run check:retrieval && npm run build && npm run shots",
    "shots": "node scripts/shots.mjs",
    "verify:live": "npm run typecheck && npm run check:content && npm run check:retrieval && npm run build && node scripts/verify.mjs"
  },
  "dependencies": {
    "geist": "^1.7.2",
    "gray-matter": "^4.0.3",
    "next": "16.3.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@tailwindcss/postcss": "^4.1.0",
    "@types/node": "^22.10.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "axe-core": "^4.13.0",
    "playwright": "^1.62.1",
    "tailwindcss": "^4.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
