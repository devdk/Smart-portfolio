# Where Build-With-Me submissions are delivered.
# Any endpoint that accepts a JSON POST: Resend, Formspark, Zapier, a webhook.
# Without it the form still succeeds and logs server-side, so a preview
# deployment never looks broken to whoever is testing it.
CONTACT_WEBHOOK_URL=
